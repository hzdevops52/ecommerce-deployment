apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  namespace: ecommerce
spec:
  type: ClusterIP
  selector:
    app: mongodb
    namespace: ecommerce
  ports:
  - port: 27017
    targetPort: 27017
